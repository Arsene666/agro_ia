name: agro_ia
description: Projet agro IA

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Packages UI
  cupertino_icons: ^1.0.2
  google_fonts: ^6.3.2
  fl_chart: 0.66.2  # Version compatible avec Flutter SDK

  shared_preferences: ^2.3.0
  
  # State management
  provider: ^6.1.2
  
  # HTTP & Networking
  http: ^1.2.0
  
  # File system
  path_provider: ^2.1.3
  
  # Internationalization - version compatible
  intl: ^0.19.0  # Utilisez 0.19.0 au lieu de 0.20.2
  firebase_core: ^4.4.0
  firebase_database: ^12.1.2
  
  # SUPPRIMEZ ou commentez ces lignes :
  # vector_math: ^2.2.0
  # meta: ^1.18.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

firebase_core:
  platforms:
    android:
    ios:
    web:
